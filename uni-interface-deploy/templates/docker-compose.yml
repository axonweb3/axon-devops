version: '3'

services:
  uni-interface:
    image: nginx:latest
    container_name: uni-interface-v2
    volumes:
      - ./build:/usr/share/nginx/html
    ports:
      - "8513:80"
    restart: on-failure