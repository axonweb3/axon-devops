version: "3"

services:
  uni-interface:
    image: nginx:latest
    container_name: uni-interface_v3
    volumes:
      - ./build:/usr/share/nginx/html
    ports:
      - "8645:80"
    restart: on-failure
    networks:
      - uni-net
networks:
  uni-net:
