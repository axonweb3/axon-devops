version: "3"

services:
  uni-interface:
    image: nginx:latest
    container_name: uni-interface_v3
    volumes:
      - ./build:/usr/share/nginx/html
    ports:
      - "listen_on_port:80"
    restart: on-failure
    networks:
      - uni-net
networks:
  uni-net:
